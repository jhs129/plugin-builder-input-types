{
  "name": "builder-input-types",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "dev:plugin": "pnpm --filter @jhsdc/builder-input-types dev",
    "build:plugin": "pnpm --filter builder-plugins build && pnpm --filter @jhsdc/builder-input-types build"
  },
  "devDependencies": {
    "turbo": "^2.5.5",
    "typescript": "^5"
  }
}
